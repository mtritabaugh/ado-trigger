trigger: none

resources:
  containers:
  - container: petclinic-ado
    image: mtritabaugh/petclinic:latest
    endpoint: dockerhub
    mapDockerSocket: true
    mountReadOnly: 
      externals: false
      tasks: false
      tools: false
      work: false


stages:
- stage:
  - jobs:
    - job:
      - script: echo 'hello'
