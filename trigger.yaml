trigger: none

resources:         
  containers:
  - container: petclinic
    type: Docker
    azureSubscription: dockerhub
    registry: mtritabaugh
    repository: petclinic
    image: petclinic
    trigger: 
      tags:
        include: 
        - "*"
